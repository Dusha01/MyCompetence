 svelte.config.js

import adapter from '@sveltejs/adapter-static';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
  kit: {
    adapter: adapter({
      pages: 'build',  // Теперь сборка будет в папке build
      assets: 'build',
      fallback: 'index.html',
      precompress: false
    }),
    appDir: 'app'
  },
  preprocess: vitePreprocess()
};

export default config;

Также нужно сделать svelte.config.js
